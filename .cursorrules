# Cursor command denylist
# Format: DENY command_name [reason]

# Deny potentially dangerous system commands
DENY rm -rf /* Dangerous recursive deletion
DENY sudo Elevated privileges not needed
DENY chmod 777 Overly permissive file permissions
DENY docker system prune -a Dangerous container cleanup

# Deny sensitive operations
DENY git push --force Force pushing can overwrite history
DENY git reset --hard Destructive git operation

# Add more denied commands as needed