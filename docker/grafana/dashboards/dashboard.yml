# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'file-connector-dashboards'
    orgId: 1
    folder: 'File Connector'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards